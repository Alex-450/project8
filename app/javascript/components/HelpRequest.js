import React from "react";
import { Button } from "react-bootstrap";

function HelpRequest(props) {
  return (
    <div>
      <p>Help request view to go here</p>
      <Button>Submit a help request</Button>
    </div>
  );
}

export default HelpRequest;
